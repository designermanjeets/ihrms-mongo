import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ihrms.app',
  appName: 'iHRMS-Native',
  webDir: 'dist/apps/i-shell',
  bundledWebRuntime: false
};

export default config;
